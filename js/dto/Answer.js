export default class Answer {
  constructor(id, value, title, sorting, type) {
    this.id = id;
    this.value = value;
    this.title = title;
    this.sorting = sorting;
    this.type = type;
  }
}
